# CLI Reference

The `rpgleveler` command provides a command-line interface for leveling up Basic
Fantasy RPG characters.

---

## Command Help

The CLI arguments can be viewed with:

```bash
rpgleveler --help
```

Example output:

```bash
{{ run("rpgleveler --help") }}
```

---


